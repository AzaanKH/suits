# Convex backend

Run `pnpm convex:dev` to connect a development deployment and regenerate
`convex/_generated`. Run `pnpm convex:seed` in a second terminal to replace the
development deployment's storefront data with the local seed set.

See `../docs/convex-schema.md` for the schema rationale and seed behavior.
